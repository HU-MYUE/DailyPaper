import user from './user';
import group from './groups';
import paper from './paper';


/**
 * 路由汇总 后续路由请追加在routers数组中
 * 如： const routers = [user, group, paper];
 * @type {Router[]}
 */

const routers = [user, group, paper];

export default routers;
